# 白板题③ 开考前的背景（先读这个，再翻规格书）

**这份是 recap，不是新方法。** 各向同性方向抽样在 **8/31 第 2 课 `lesson02_direction.py`** 里
已经完整讲过一遍（含立体角推导 + 错误示范 + 分箱实测）。
所以这张卷子考的是**你还记不记得、能不能自己推**，不是「没教过就考」。

**卷子里只有两处是新连接，都在下面这份背景里交代清楚：**
- (6c) 用**昨天刚练的舍选法**去掉三角函数 —— 方法是昨天的，场景是新的
- (7) 通向**今天步骤 1**（数随机数消耗）—— 这是预热，不是新知识

---

## 1. 方向是什么

三维里一个粒子的方向是一个**单位向量**：

```
Ω = (u, v, w),   u² + v² + w² = 1
```

`u, v, w` 叫**方向余弦**（它们分别是 Ω 与 x/y/z 轴夹角的余弦）。

**各向同性** = 方向均匀分布在单位球面上，没有任何方向被偏爱。

---

## 2. 🔴 那个所有人第一次都会掉进去的坑

直觉做法：

```
θ 均匀抽 [0, π]        φ 均匀抽 [0, 2π)
w = cos θ,  u = sin θ cos φ,  v = sin θ sin φ
```

**这是错的。**

**为什么**：球面上的面积元（立体角元）是

```
dΩ = sin θ · dθ · dφ
```

那个 `sin θ` 因子的意思是：**同样宽的一条 dθ 带，在赤道附近面积大，在两极附近面积小。**
均匀抽 θ 等于给每条带**相同的权重**，于是小面积的两极带被喂了过多的粒子 —— **粒子堆在两极。**

---

## 3. 正确做法：换元把 sin 吸收掉

令 `μ = cos θ`，则 `dμ = −sin θ dθ`，于是

```
dΩ = dμ · dφ
```

**sin 因子没了。** 所以：

```
μ  均匀抽在 [−1, 1]
φ  均匀抽在 [0, 2π)
```

这就是「**抽 μ 不抽 θ**」这条铁律的来源，每本 MC 教材上都有。

组装回方向余弦：

```
w = μ                       （因为 μ = cos θ，而 w 就是 cos θ）
sin θ = √(1 − μ²)           （θ ∈ [0, π] 上 sin 恒非负 ⟹ 取正根）
u = sin θ · cos φ
v = sin θ · sin φ
```

---

## 4. 几何说法：阿基米德帽盒定理

球面被两个**平行平面**截出的一条带，它的面积**只跟两平面的间距有关**，与带在球上的高低无关。

> 换句话说：**球面在 w 轴上的投影是均匀的。**
> 「μ 均匀」不是一个技巧，是球面自己的性质。

---

## 5. 8/31 实测记录（那天的结论，别忘了）

`check()` 打了四组数：`|Ω|` 偏离 1 的量、`<u><v><w>`、`<u²><v²><w²>`、**w 的 10 个分箱**。

- **`<u>,<v>,<w>` 在对法和错法上都 ≈ 0**
- **`<u²>` 也不容易一眼看出问题**
- **唯一暴露错误的是 w 的分箱**：错法两端明显比中间多

> 🔴 **那天的结论：判断抽样对不对，光看均值往往看不出来，一定要看分布本身。**
> 这条和 9/16「一条判据如果在错误的实现上也通过，它就没有分辨率」是同一件事。

---

## 6. 🔴 一条通向今天步骤 1 的线（这是新的）

`lesson03_tally.py` 是**一维平板**。粒子只沿 z 走：

```
z += s * w
```

**`u` 和 `v` 一次都没被用到。** 那么一个自然的问题：**既然用不上，能不能干脆不抽 φ？**

这个问题有**两个答案，分属两个层面**，而它们可能不一致 —— 卷子的 (7) 问的就是这个。
**今天步骤 1（数 `lesson03_tally.py` 的随机数消耗顺序）正是这条线的正式版本。**

---

## 7. 昨天那道题（舍选法）在这里能派上用场

`cos φ` 和 `sin φ` 各要算一次三角函数。在 GPU 上三角函数不便宜。

**昨天刚练的舍选法给了另一条路**：不去抽角度 φ，而是直接在**单位圆盘**里抽一个点，
用它的坐标现成地当作 `cos φ` 和 `sin φ`（归一化之后）。

- 抽 `(x, y)` 各均匀在 `[−1, 1]`
- 若 `x² + y² > 1` 就丢掉重来（**这就是舍选**）
- 留下来的点方向在圆周上是均匀的

**卷子 (6c) 要你自己把这条路补完，并算出它的接受率。**
接受率的算法和昨天 (2a) 完全一样 —— **一个面积比**。

---

## 8. 开卷前的三条纪律（今天的）

1. **验证上限 5 条**，每条旁边写一行「这条抓什么」
2. **判据的两边必须来自两条独立的路**（9/17 恒真 assert 第六次）
3. 🔴 **交卷前自己先过一遍「我这几个答案彼此对得上吗」** —— 9/17 一天四次自相矛盾，这是 (8) 直接考的东西
