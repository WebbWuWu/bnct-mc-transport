# 加餐：层数扫描实验（加餐菜单第 3 条）

**2026-09-09。前置：主线已完成。加餐一次只做一条 —— 今天做的就是这一条。**

---

## 为什么做这个

9/8 画完图，Q2 你答「case2/case3 表层的峰大概在第二层，两个 case 大致一样」。
**那张图给不出这个结论**：case2 的 dz=0.1，峰落在 z≈0.15；case3 的 dz=0.25，峰落在 z≈0.375。
**20 层分箱看不见峰的真实位置** —— 分箱宽度本身就是你的分辨率下限。

这个实验直接回答：**峰到底在哪？把层数加到 100，峰会往哪边挪？**

同时它顺手做掉一件重构，而且**重构的判据是现成的**（`before_lesson03.txt` 就摆在那儿）。

---

## 第一步：重构（这一步有免费的判据，先做）

现在 `NL=20` 是第 142 行的**模块级全局变量**，`run_slab` 直接读它。

改成：

```python
def run_slab(N, seed, d, sigma_t, sigma_s, NL=20):
```

函数体里那个 `NL` 就自动变成参数（局部名遮住全局名），**一个字都不用改**。

> 这和 9/8 给 `print_flux_table` 加 `csv_path=None` 是**同一个动作**：
> **加一个带默认值的参数 = 加功能而不破坏任何已有调用。**
> 三处 `run_slab(N,2026,...)` 一行不改，因为默认值就是 20。

**判据（必须先过这一条才许往下走）：**

```
python -u lesson03_tally.py > after_NL.txt
Compare-Object (Get-Content before_lesson03.txt) (Get-Content after_NL.txt)
```

**必须 0 处不同。** 有一处不同就说明你顺手改了别的东西 —— 9/8 那 114 行注释就是这么没的。

⚠️ 不要用 `git diff --no-index`：PowerShell 的 `>` 写的是 UTF-16，那条判据会退化成 "Binary files differ"。

---

## 第二步：先预测，再跑（**顺序不许反**）

**把下面四问的答案写在纸上，写完了才许敲代码。** 跑完再倒推答案的，这一条加餐等于没做。

**Q1.** 层数从 20 加到 100（`dz` 从 0.1 缩到 0.02），**某一层的相对误差 R 会变大还是变小？大约按什么规律？**
提示，想清楚两件事：一条历史穿过一个薄层，贡献的径迹长是 `dz/|w|`；而 `w` 很小的那些历史贡献很大 —— 但它们真的能穿过去吗？

**Q2.** `sum(phi_k * dz_k)` 这个量在 NL = 10 / 20 / 50 / 100 四次里应该怎么变？
先说清楚**它的物理意义是什么**，再说它该不该变。**这一问的答案决定了它是第几档判据。**

**Q3.** case2（d=2, Σt=1, Σs=0.8）的通量峰，20 层时读出来在 z≈0.15。
**100 层时你预测峰在哪？往表面挪还是往深处挪？**

**Q4.** 运行时间随 NL 怎么变？**线性增长、不变、还是别的？** 说出理由。

---

## 第三步：跑

只跑 **case2**（d=2.0, Σt=1.0, Σs=0.8, N=1e6, seed=2026）。四个 NL：**10 / 20 / 50 / 100**。

写一个小驱动，每个 NL 打印**三个数**就够：

| 打印什么 | 怎么算 |
|---|---|
| `sum(phi_k * dz)` | 你自己写，别用 `print_flux_table` 的返回值（它没返回） |
| 最大的那层 R | `max(err_k / phi_k)` |
| 峰所在层的 `z_mid` | `phi` 最大的那个 k，`z_mid = (k+0.5)*dz` |

再打印一行 `time.perf_counter()` 的耗时 —— 那是 Q4 的判据，也是**第 12 课立 CPU 基线**要用的同一个东西，提前碰一次。

⚠️ **驱动写成一个 `for NL in [10,20,50,100]:` 循环，不要复制粘贴四遍。**
四份复制粘贴的代码，改一个地方要改四处，第五处忘了改就是下一个静默 bug。

---

## 判分表

| # | 判据 | 过 |
|---|---|---|
| 1 | 重构后 `Compare-Object` **0 处不同** | |
| 2 | 四个 Q 的预测**写在跑之前**（纸上有痕迹） | |
| 3 | Q1 方向对（变大/变小），量级不离谱（实测在预测的 0.5×～2× 内） | |
| 4 | Q2 说得出 `sum(phi_k*dz)` 的物理意义 | |
| 5 | Q2 实测：四个 NL 下这个数一致到多少位？**你能不能给它定一个容差** | |
| 6 | Q3 预测的方向对 | |
| 7 | 驱动是循环，不是四份复制粘贴 | |
| 8 | 跑完能回答：**「峰在哪」这个问题，需要多少层才算答清楚？** | |

**6 条以上算过。**

---

## ⚠️ 三个陷阱（跑完再看）

<details>

**陷阱 A：`sum(phi_k*dz)` 不会逐位相同。**
它是恒等式，但求和顺序变了（10 项 vs 100 项），**浮点重结合会让它在第 14~16 位上不一样**。
所以判据不能写 `==`，要写一个容差 —— **而那个容差你必须说得出是怎么来的**（相对误差 ~1e-14 量级，不是随手拍的 1e-6）。
这正好是 9/8 那条「重结合即使发生了也小到不影响输出」的**正面版本**：那次判据分辨率不够看不见它，这次你会亲眼看见它。

**陷阱 B：峰的位置不是「phi 最大的那一层」。**
你打印的是**层平均通量**，峰的真实位置在那一层内部的某处。层越薄，`z_mid` 越接近真峰；但它永远只是一个上界精度为 `dz` 的估计。
**答 Q3 时要把这句话说出来** —— 「峰在 z=0.13」和「峰在 0.12 到 0.14 之间」是两句强度完全不同的话。

**陷阱 C：R 变大不等于结果变差。**
薄层的 R 大，是因为每层的样本少了，不是因为程序退化了。
**要比的是同一个物理量**：`sum(phi_k*dz)` 的误差在四个 NL 下应该基本不变 —— 总信息量没变，只是切得更碎。

</details>
