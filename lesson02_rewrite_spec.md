# 第2课 · 无脚手架重写规格书

**规则**：新建 `lesson02_slab_v2.py`，从**完全空白**开始。
不看 `lesson02_slab.py`，不看 `lesson02_direction.py`，不看第1课的任何文件，不看这份文档以外的东西。
卡住了就停下来想，想不出来就记下"卡在哪"，**不要去翻旧文件**。

下面只给规格，不给任何一行代码，也不给公式。

---

## 要实现的东西

一块厚度 d 的均匀平板，z 从 0 到 d，x/y 无限大。
中子从 z=0 垂直射入（初始方向 +z）。
统计三个互斥且穷尽的结局：透射 T、反射 R、吸收 A。

## 你必须自己写出来的部件

1. **LCG 随机数发生器**：乘数 1664525，增量 1013904223，模 2^32。
   要有一个返回 [0,1) 浮点数的方法。

2. **各向同性方向抽样**：返回单位向量 (u, v, w)。
   自己想清楚该抽哪个量、为什么不能均匀抽极角。

3. **单条历史的循环**：
   - 抽飞行距离（用逆变换）
   - 推进位置
   - 出界判断
   - 碰撞时按散射截面占比决定散射还是吸收
   - 散射后换新方向，继续
   - 加一个最大事件数上限防死循环

4. **新增（旧版没有的）**：统计**每条历史的平均碰撞次数**，随 T/R/A 一起返回并打印。

## 三个 case

| case | d | Sigma_t | Sigma_s |
|---|---|---|---|
| 1 | 2.0 | 1.0 | 0.0 |
| 2 | 2.0 | 1.0 | 0.8 |
| 3 | 5.0 | 1.0 | 0.9 |

N = 1000000，seed = 2026。

## 自检标准

- 三个 case 的 T+R+A 都必须等于 1
- case 1 的 R 必须严格等于 0
- case 1 的 T 必须对上 exp(-2) = 0.135335（差值在 3 倍标准误内）
- 触顶最大事件数的历史数必须是 0
- 平均碰撞次数：case 1 应略小于 1，case 2 约 2.2，case 3 约 5.2

## 写完之后

用 diff 对比 v2 和旧版，看你哪里写得不一样。
不一样不等于错——但每一处不一样你都要能说清为什么。
