# 数模四天技巧复盘

**填写日期**：2026-09-15（比赛 9/10–9/13，9/14 额度用完未开工）
**填写方式**：用户口述，Claude 打字入库。

---

## 一、这四天用到了哪些项目里还没用过的东西

**答：一个都没有。**

| 东西 | 用到了吗 | 第 5 课用得上吗 |
|---|---|---|
| numpy 数组 / 向量化 | ❌ 没用到 | ✅ 要用，**从零开始** |
| pandas 读表 | ❌ 没用到 | 暂不用 |
| matplotlib（比 9/8 更进一步的用法） | ❌ 没用到 | ✅ 要用 |
| dict | ❌ 没用到 | ✅ 要用（反应道 MT 号 → 截面） |
| try / except | ❌ 没用到 | 读文件时会遇到 |

## 二、四天里卡最久的一个 bug

**答：没有 bug。程序并不复杂，且都经过了多轮验证。**

## 三、这四天里我做的判据

**答：多轮验证。**（具体形式未记录）

## 四、一句话交接给 9/15 的自己

> 比赛没有给第 5 课留下任何垫脚石，numpy / matplotlib / dict 全部按「零基础」处理。

---

## 五、这份复盘的真实价值（Claude 记录，2026-09-15）

原计划写这份复盘是为了「把四天里摸过的东西接到第 5 课」。**这个预期落空了，而落空本身是有用的信息。**

### 落空的直接后果：改排期

任务表 v3 里第 5 课的前置假设是「数模四天你一定会用到 numpy 和 matplotlib，那正是你现在零覆盖、第 5 课立刻要用的两样」。

**这个假设不成立。** 所以第 5 课的实际内容比表上写的多一块：**numpy 要从头讲**，不是复习。

→ **处置：第 5 课的日期区间（9/14–9/20）保持不动，但内部重新分配——numpy 不再当作「顺带学会」，而是单独占一个台阶。**
→ 如果 9/20 收不了口，按 v3 的纪律报「本周欠了几天」，不加班补。

### 一句存疑（记下来，不追）

> 「没有 bug」这句话，在本项目的日志里出现过等价形式，而且每次都被后来的判据推翻过：
> - 9/7 `whiteboard_05_scaffold.py`「三条自测全过」→ 实际 `E[k+1]` 越界
> - 9/9 重写版「三条全过」→ 实际返回 `None`
> - 9/8 lesson03 对拍「0 处不同」→ 实际是判据看不见第 16 位的差异
>
> **本项目已确立的结论是：「没发现 bug」和「没有 bug」是两句话，中间隔着判据的分辨率。**
> 数模的代码已经交了，现在回头查没有收益，**所以这条不追**。
> 记在这里只是因为 12 月那道必答题问的恰好是这件事——到时候的回答不能是「我的程序没出过 bug」，
> 而应该是「我的程序出过 50+ 个 bug，我能说出每一个是怎么被哪条判据抓到的」。
> **后者才是这个项目真正的资产，而它只存在于本项目的日志里，不在数模里。**
