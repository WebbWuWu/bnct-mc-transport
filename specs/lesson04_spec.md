# 第 4 课规格书：弹性散射运动学与慢化

**文件**：`lesson04_energy.py`
**规则**：无脚手架。补全关掉。这份文件只给规格和判据，不给任何代码结构。
**日期**：2026-09-04

---

## 0. 这一课在整个项目里的位置

到第 3 课为止，你的中子是**单能**的，截面是常数。这是个玩具。

真实物理里，中子的截面强烈依赖能量：

- **BNCT**：源是超热中子（~10 keV），必须在组织里慢化到热能（0.025 eV），
  ¹⁰B 的 (n,α) 截面才够大（热中子处 3838 barn，1/v 律）。剂量分布 = 慢化过程的分布。
- **聚变**：DT 中子出生在 14.1 MeV，要在锂包层里慢化 + 增殖。
  ⁶Li(n,α)t 也是 1/v 的热截面主导，⁷Li(n,n'α)t 是阈值反应（阈值 ~2.8 MeV）。
  **TBR 是这两个反应在能谱上竞争的结果** —— 没有能量，就没有 TBR。

所以这一课是从"玩具"到"物理"的那一步。两个方向的核心都在这里。

**但这一课只做能量。不要碰几何，不要碰 tally，不要把第 3 课的板拿过来。**
无限介质、单核素、常数截面。只验能量。几何和能量合体是第 6 课的事。

---

## 1. 物理：为什么要跑到质心系去

中子和静止靶核弹性碰撞。实验室系里这是个两体问题，角度和能量耦合在一起，很难直接抽样。

到**质心系**（CM）里，碰撞前后两个粒子的动量大小都不变，只是整体转了个角。
于是"散射多剧烈"这件事被压缩成一个变量：CM 系散射角 `θ_cm`。

而对低能中子（BNCT 全程、聚变的慢化段），**s 波散射主导**，CM 系里散射是各向同性的。
各向同性 ⟹ `μ_cm = cos θ_cm ~ U(-1, 1)`，一行代码就抽完了。

这就是全部的技巧：**在难的坐标系里抽样很难，在对的坐标系里抽样是均匀分布。**
这个思路你后面还会用到（各向同性方向抽样其实是同一个）。

### 需要用到的三个公式（不要背，要能推）

设 `A` = 靶核质量 / 中子质量（碳就是 12，氢就是 1）。

**能量比**

```
E' / E = (A² + 2·A·μ_cm + 1) / (A + 1)²
```

**实验室系散射角余弦**

```
μ_lab = (A·μ_cm + 1) / sqrt(A² + 2·A·μ_cm + 1)
```

**最大能量损失参数**

```
α = ((A - 1) / (A + 1))²
```

### 三个必须能口头回答的问题（写进注释）

**Q1.** 把 `μ_cm = -1`（正对撞回弹）代进能量比公式，化简，你会得到什么？
把 `μ_cm = +1`（擦边）代进去呢？由此说明为什么能量比的取值范围是 `[α, 1]`。

**Q2.** `A = 1`（氢）时 `α = 0`。这在物理上是什么意思？
为什么中子撞氢可以一次损失掉**全部**能量，撞碳却最多只能损失 28.4%？
（提示：想想台球撞台球 vs 台球撞保龄球。）

**Q3.** `μ_lab` 的公式里，分母恰好是能量比公式的分子（差个 `(A+1)²`）。这是巧合吗？
从矢量合成的角度说一句为什么。

---

## 2. 要实现的东西

最少三样，函数边界自己划：

1. **一次散射**：输入 `(E, A, rng)`，输出 `(E_new, mu_lab)`。
   这是唯一涉及随机数的地方。

2. **解析量**：`α(A)`、`ξ(A)`、`⟨μ_lab⟩(A)`。纯函数，不碰 rng。

3. **验证**，见下（规格五道，实际做到九道）。

**必须踩的两个坑（提前告诉你，因为它们会静默出错，不会报错）**

- `A = 1` 且 `μ_cm = -1` 时，`μ_lab` 的分母是 `sqrt(0) = 0`。除零。
  这个点的概率是零，但浮点抽样迟早会抽到贴着它的值，`μ_lab` 会炸成 ±inf 或 nan。
  **你要决定怎么处理，并在注释里写明理由。**
  （物理上：`E' = 0` 的中子已经不存在了，`μ_lab` 无定义。）
- `ξ(A)` 的公式里有 `α·ln(α)`。`A = 1` 时 `α = 0`，`ln(0)` 是 `-inf`，
  `0 · (-inf)` 在 Python 里是 `nan`，不是 0。**必须单独处理 A=1。**
  数学上 `lim_{α→0} α·ln(α) = 0`，所以 `ξ(1) = 1` 是正确的极限值，不是特例硬编码。

写完这两处，各写一条 assert 把它们钉死。

---

## 3. 验证（规格要求五道；2026-09-05 实际做了九道，全部要过）

`α = ((A-1)/(A+1))²`。两个测试核素：**H (A=1)** 和 **C (A=12)**。

### 验证 1 — 能量比范围（恒等式，强度：硬边界）

抽 N 次散射，每次检查

```
α - 1e-12  ≤  E'/E  ≤  1 + 1e-12
```

| A | α |
|---|---|
| 1 | 0 |
| 12 | 0.7159763314 |

这条**几乎抓不到 bug**（范围检查太宽），但它是免费的，且能立刻抓住符号错和 `(A+1)²` 写成 `A²+1`。
写它的时候就要意识到：**这是最弱的一条**。第 3 课的教训——最弱的测试要认出来，不能当成保险。

### 验证 2 — 能量比在 [α, 1] 上均匀（本课最值钱的一条）

**CM 系各向同性 ⟺ 实验室系能量比均匀。** 这是一个严格的等价关系，因为
`E'/E` 是 `μ_cm` 的**线性**函数，均匀分布经线性变换还是均匀分布。

做法：把 `E'/E` 归一化到 `[0,1]`：`t = (E'/E - α)/(1 - α)`，
分 `M = 20` 个等宽箱，做卡方检验：

```
χ² = Σ_j (O_j - E_j)² / E_j ,   E_j = N/M ,   自由度 = M - 1 = 19
```

判据：`χ² < 43.82`（19 自由度，0.999 分位）。N 取 10⁶。

**为什么这条最值钱**：验证 1 只管边界，验证 3/5 只管一阶矩。
只有卡方管**整个分布形状**。如果你把 `μ_cm` 抽成了 `U(0,1)` 而不是 `U(-1,1)`，
验证 1 会过（范围仍在 [α,1] 内的一半），验证 3、5 会偏但你可能拍成"统计涨落"，
**只有卡方会当场炸掉**。

写完这条，按老规矩问自己两句：什么样的 bug 会让它失败？什么样的错误能从它底下溜过去？
（第二问的答案：把每次的 `μ_cm` 复用同一个随机数 —— 分布对，但相邻抽样相关。卡方看不见相关性。）

### 验证 3 — 平均对数能降 ξ（统计量，3σ 判据）

```
ξ ≡ ⟨ ln(E/E') ⟩ = 1 + α·ln(α) / (1 - α)
```

| A | ξ 解析值 |
|---|---|
| 1 | 1.000000 |
| 12 | **0.157768** |

> ⚠️ **进度日志里写的 0.1589 是错的**，重算一遍：
> α = (11/13)² = 0.7159763314，ln α = −0.3341073，
> α·ln α = −0.2392140，1−α = 0.2840237，比值 = −0.8422332，ξ = 0.1577668。
> 教科书上碳的 ξ 通常印作 0.158，与此一致。收工时把日志改过来。

判据：`|⟨ln(E/E')⟩ - ξ| < 3·SE`，`SE` 用 per-sample 的方差算（老规矩：均值可以从总账算，**方差必须 per-sample**）。

### 验证 4 — 慢化：四个版本（4a / 4a′ / 4b / 4b′），强度各不相同，都要写

**4a（严格版，Wald 恒等式）**

固定碰撞次数 `n = 50`，每条历史从 `E0` 出发**恰好散射 50 次**，记录 `ln(E0/E_50)`。
因为每次的 `ln(E/E')` 独立同分布、期望是 `ξ`，所以

```
⟨ ln(E0/E_50) ⟩ = 50 · ξ      （严格等式，不是近似）
```

这是**能给 3σ 判据的版本**。用它。

**4a′（方差恒等式，追加于 2026-09-05）**

同一批 `ln(E0/E_50)` 样本，再判一条：

```
Var(u_50) / (50 · Var(x)) ≈ 1
```

均值恒等式 `⟨Σx⟩ = Σ⟨x⟩` 只用到期望的**线性性**，对相关样本照样成立。
所以验证 1/2/3/5/4a 全都看不见「随机数被复用 / 相邻抽样相关」。
**只有方差恒等式看得见**，而左右两边你都已经算过，代价只是一次除法。

容差：`SE(s²)/s² ≈ √(2/N_hist)`。N_hist=50000 → 0.63%，3σ 带宽 ±1.9%。
**拍 ±3%。不许拍 ±5% —— 那是 8σ，等于白写。**

**4b（工程版，普适界）**

从 `E0 = 2 MeV` 出发，一直散射到 `E < E_th = 0.025 eV`，数碰撞次数 `n`。
教科书公式（naive）：

```
n ≈ L / ξ ,     L = ln(E0/E_th) = 18.197537
```

**这条是近似，不许写成 3σ assert。** 最后一次碰撞会**冲过头** —— 中子不会正好停在
0.025 eV，而是掉到它下面某处，所以实测均值会**系统性偏大**。

> 🔴 **判据订正（2026-09-06）**：旧版这里写「容差拍成 ±5%」，**是错的，A=1 必炸**。
> 错因是把超越量估成了「约半次碰撞」。正确的超越量均值是 `E[x²]/(2·E[x])`，
> 它和 `E[x]/2` 只在 x 是常数时才相等。A=1 时 `x ~ Exp(1)` **无记忆**，
> 超越量仍是 Exp(1)，均值**整整 1 次碰撞**。
> 实测偏差：A=1 **+5.50%**（18.1975 → 19.2314），A=12 **+0.60%**（115.3429 → 116.1018）。

**正确的判据形状**：超越量以**碰撞数**为单位，对**任何 A** 都被夹在 `[2/3, 1]` 之间：

```
2/3 − 3·n_se  ≤  n_mean − L/ξ  ≤  1 + 3·n_se
```

上界 1 = A=1（指数分布，无记忆）；下界 2/3 = A→∞（`x ≈ 1−r`，近似均匀分布）。
中间单调递减，**没有第三种情况**。

**这个形状比 ±5% 好在三处**：

1. **标度不变** —— 判据写成**差**不是**比**。`⟨n⟩ − L/ξ = E[x²]/(2ξ²)`，只依赖 A，
   不含 `L`。换 14.1 MeV 源不用改判据。（相对偏差里含 `L`，L 一变大容差就失效。）
2. **判据里不含任何 A 专属数字** —— 没有「答案」，只有普适常数。
3. **更锐** —— `3·n_se ≈ 0.1`，而区间宽 0.33；±5% 对 A=12 相当于 ±9 次碰撞，钝了近百倍。

⚠️ **三个易错点（9/5 全踩过）**：判据是**差**不是**比**；`3·n_se` 那层统计余量不能省
（`[2/3, 1]` 是**期望值**的界，不是样本均值的界）；**不许加 `abs()`**
—— 超越量定义上非负，加了 abs 之后 `−0.8` 这种物理上不可能的结果反而会通过。
（老规矩「`abs()` 不能漏」针对的是**双边**统计检验；4b 是**单边物理界**。
**规矩不是"永远加 abs"，是"判据的形状要和物理的形状一致"。**）

**4b′（更新理论，追加于 2026-09-05）**

把超越量算进去，模型误差就消失了，于是**同一个物理量**可以给**严格 3σ 判据**：

```
⟨n⟩ = ( L + E[x²]/(2ξ) ) / ξ

E[x²] = [ 2 − α·((lnα)² − 2·lnα + 2) ] / (1 − α)      α=0 时取 2（不是 0）
```

⚠️ `E[x²]` 在 α=0 的极限是 **2**：趋于 0 的是 `α·(lnα)²` **那一项**，不是整个分子。
写成 0 的后果是静默的 —— A=1 的 renew 退化成 naive，**4b′ 和 4b 变成同一条，且不报错**。

> **同一个物理量，判据强度取决于你拿它对哪个公式。**
> 对 naive → 只能给第 4 档工程界；对 renew → 能给第 3 档 3σ。
> 实测：A=1 **+1.13σ**，A=12 **+1.58σ**。

**区分 4a / 4a′ / 4b / 4b′ 的能力，比会写这四条更重要。**
第 3 课的教训是「三种强度不能混」，这里出现了第四档：**工程估计与普适界**。
第四档的容差**不是误差棒，是模型误差预算** —— 必须 **>** 已知模型误差（否则永远炸），
同时 **<** 真 bug 造成的偏差（否则什么都抓不到）。

---

### 验证 5 — 前向散射偏置（统计量，3σ 判据）

```
⟨μ_lab⟩ = 2 / (3A)
```

| A | ⟨μ_lab⟩ |
|---|---|
| 1 | 0.666667 |
| 12 | 0.055556 |

**这条不是数值游戏，它是整个慢化剂理论的物理内核**：

`A` 越大，`⟨μ_lab⟩` 越接近 0（各向同性），但**每次能量损失越小**（α 越接近 1）。
`A` 越小，中子被弹得越靠前（`⟨μ_lab⟩` 大），但**一次就能剥掉大部分能量**。

慢化能力看的是 `ξ·Σs`（慢化本领），减速比看 `ξ·Σs/Σa`。
**所以慢化剂要用轻核**（H、D、C），这就是水、重水、石墨的由来。

而 **8/31 那个教学点在这里闭环**：人体富氢 → c 接近 1 → 碰撞次数暴涨 →
现在你还知道了另一半：富氢同时意味着**每次碰撞能量损失巨大**（α=0），
所以中子在人体里既碰得多、又降得快 —— 这两件事**都**让 BNCT 的剂量分布强烈依赖精确模拟，
也**都**是要上 GPU 的理由。

---

## 4. 参数与再现性

- 随机数：复用 `lesson03_tally.py` 里你自己写的 `lcg`。**不许用 `random` 模块。**
- `seed = 2026`，跟前三课一致。
- N：验证 1/3/5 用 `N = 200000`；验证 2 用 `N = 1000000`（卡方要样本量）。
- `E0 = 2.0e6` eV，`E_th = 0.025` eV。单位统一用 eV，在文件头注明。

---

## 5. 卡住的地方记在这里

（自己填）

---

## 6. 做完之后（不要提前做）

- 把 A 扫一遍：1, 2, 9, 12, 16, 238，打一张 `A | α | ξ | ⟨μ_lab⟩ | 慢化到热能的碰撞数` 表。
  ²³⁸U 那一行会让你直观看到为什么快堆里中子能保持快。
- 想清楚：这一课的散射是**各向同性 CM**。真实的 14.1 MeV 中子在 CM 系是**各向异性**的
  （高能下 s 波不再主导），要从 ENDF 的 MF=4 读角分布。这是第 5 课之后的事，先记着。
