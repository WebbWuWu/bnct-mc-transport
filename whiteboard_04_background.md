# 白板题④ 背景讲义：用蒙卡估一个确定的数

**出题日：2026-09-19（周六·大块日）。这是 9/18 欠下的那一道。**
**读完这份再翻 `whiteboard_04_spec.md`。讲义里没有规格书任何一问的答案。**

---

## 0. 这道题不是新东西，是三样旧东西的合流

| 你已经有的 | 哪天拿到的 | 这道题要用它干什么 |
|---|---|---|
| 舍选法：往框里均匀撒点，看落不落进目标区域 | 白板题⑧（9/17） | 撒点的机器 |
| 单位圆盘舍选实测接受率 **0.78527**，π/4 = 0.78540 | 9/18 白板题③(6c) | 你其实**已经估出过 π 一次了，只是当时没意识到** |
| `mean_se(s, sq, n)` 和「误差 ∝ 1/√N」 | 第 2 / 4 课 | 给这个估计值配误差棒 |

**唯一真正新的一件事**：前五课你都在估**一条曲线**（通量随 z 的分布）。
这道题估的是**一个确定的数**。

> 🔴 估一个确定的数，好处是**真值你知道**。
> 前五课里「怎么知道结果是对的」永远要绕一圈（解析解、对拍、守恒律）。
> 这道题的真值就摆在那儿 —— 所以它是**唯一一个能把「误差棒到底准不准」本身当成被测对象**的场景。
> **这才是这道题的用处。不是为了算 π。**

---

## 1. 命中法（hit-or-miss）的一般形式

在一个**你知道面积的框** `A` 里均匀撒 N 个点，数落进目标区域 `D` 的个数：

```
p_hat = 命中数 / N        ->      |D| ≈ |A| · p_hat
```

**为什么成立**：给每个点定义一个**指示变量**

```
X_i = 1   第 i 个点落在 D 里
X_i = 0   没落在
```

那么 `E[X] = P(落在 D 里) = |D| / |A|`。

> 🔴 **这一步用的还是第 1 课那条性质**：均匀分布落在某个集合里的概率 = 那个集合的测度占比。
> 一维叫「区间长度」（`-ln(1-ξ)/Σt` 和累加抽道用的都是它），二维叫「面积占比」。
> **逆变换、离散抽样、舍选、命中法 —— 到今天为止四个方法共用同一条地基。**
> 规格书 (1) 要你把「哪一步用了它」指出来，指的就是这个。

---

## 2. 指示变量是本项目里**唯一**一个方差有闭式的量

```
X 只取 0 和 1   ==>   X² = X   ==>   E[X²] = E[X] = p
                ==>   Var(X) = E[X²] - (E[X])² = p - p² = p(1-p)
```

于是

```
SE(p_hat) = sqrt( p(1-p) / N )
```

> 🔴 **这意味着：误差棒可以在跑之前算出来。**
> 前五课的 `mean_se` 是**事后**从样本里算的。这里有第二条路 —— **事前的闭式**。
> **两条独立的路算同一个量 = 一条判据。** 这是 9/7 那条纪律在这道题上的落点。

⚠️ **但闭式里有 `p`，而 `p` 正是你要估的东西。** 两种合法用法，别混：

| 用法 | 拿什么代 `p` | 用来干嘛 |
|---|---|---|
| **事前估规模** | 一个粗略的先验值（比如 0.8） | 回答「要跑多少条才够」 |
| **事后做判据** | 用 `p_hat` 代 `p`（plug-in） | 和 `mean_se` 算出来的对拍 |

---

## 3. 一个完整算例（**故意不用 π**，免得泄题）

**目标**：估 `y = x²` 在 `[0,1]` 下方的面积，真值 `1/3`。
**框**：单位正方形 `[0,1]²`，面积 1。**命中条件**：`y < x²`。

```
N          = 1000000        （seed=20260919）
命中数      = 332526
p_hat      = 0.332526
真值        = 0.333333

SE 闭式 sqrt(p(1-p)/N)  = 4.711e-04
SE 由 mean_se 算出来     = 4.711e-04
两条路的相对差           = 5.000e-07
偏差                    = -1.71 sigma
```

**三件事要从这张表里读出来：**

1. **`-1.71σ` 是健康的。** 不是越接近 0 越好 —— 大约三分之一的运行**应该**落在 1σ 外。
   一个总是给 `+0.02σ` 的程序比这个可疑得多。
2. **两条路差 `5.000e-07`，而且这个数不是随机的。**
   `mean_se` 里有贝塞尔的 `n/(n-1)`，闭式里没有。
   `sqrt(N/(N-1)) - 1 ≈ 1/(2N) = 5e-07`。**对得上到第一位有效数字。**
   > 🔴 **两条路「差多少」本身也应该能预测。**
   > 能预测的差不叫不一致，叫**已解释的差**（9/15 那个 `p = -0.499776` 是同一件事）。
3. **`X² = X` 这一步在代码里的后果**：累加 `sq` 那一行可以整个省掉，因为 `sq == s`。
   ⚠️ 但如果你真省了，`mean_se` 那条路就**不再独立**了 —— 它和闭式变成同一条路，
   **判据当场退化成恒真。**
   > **这是「判据两边必须来自两条独立的路」在这道题上的具体陷阱。规格书 (4) 会踩到。**

---

## 4. 三个坑，只点名，不展开（规格书里会问到其中两个）

1. **框选大了会浪费**：有效样本数是 `N · p`，不是 `N`。框的面积和方差是挂钩的。
2. **常数因子会进方差**：你最后交的不是 `p_hat`，是 `常数 × p_hat`。
   `Var(cX) = c²·Var(X)` —— **误差棒要跟着乘，而且乘的是 `c` 不是 `c²`。**
3. **同一个积分有不止一种蒙卡估计量**：命中法是一种，「直接对被积函数取样本均值」是另一种。
   **两种都无偏，方差不一样。** 哪个小、为什么小，是规格书 (7)。

---

## 5. 🔴 这道题真正的落点：把 9/18 那个 0.78527 补完

9/18 白板题③(6c) 你实测单位圆盘舍选的接受率：

```
实测接受率 = 0.78527
π/4        = 0.78540
差          = 1.3e-04
```

**当时这个「差 1.3e-04」是没法判定的** —— 因为没有误差棒，你不知道 1.3e-04 算大还是算小。

> 🔴 **一个没有误差棒的蒙卡结果，不是「精度未知」，是「对错未知」。**
> 它既不能证明代码对，也不能证明代码错。**它不是一条判据，是一行日志**（9/9 原话）。
>
> **今天这道题做完，你就能回头把那一行日志变成一条判据。**

---

## 6. 与 GPU 的关系（规格书 (6) 要用）

命中法的 kernel 长这样：

```
每个线程：撒一个点 -> 算一个布尔 -> 把 0/1 加进累加器
```

和白板题⑧ 的舍选法**长得极像，但账完全不同**。
⚠️ 差别在哪、为什么两笔账不一样，规格书 (6b) 问的就是这个 —— **别把 9/17 / 9/18 那几个倍数直接搬过来。**
提示只给一句：**⑧ 里循环要转几轮是随机的；这里呢？**

---

## 7. 做题前的三条纪律（本项目现行的，不是新的）

- **验证上限 5 条**，每条旁边写一行「这条抓什么」。
- **一条 assert 有两半**：条件和消息必须说同一件事。
- **交卷前把每一问的结论句单独抄成一列，只读这一列找矛盾。**
  （9/17 一天四处，9/18 五处，其中两处在同一句话里。这一列是 (8) 的全部内容。）
