# 白板题⑤ —— 二分查找（能量网格定位）

**出题日**：2026-09-07
**形式**：白纸、黑笔、**不看电脑、不看笔记**，限时 10 分钟。写完再对判分标准。
**为什么提前到第 5 课之前**：第 5 课读 ENDF 截面表，第一件事就是「给定能量 E，找它落在网格的哪一格」。
先在纸上写对一遍，再去写代码 —— 否则你会在 h5py、插值、混合截面三件新事情之外，
再加一个「二分写错但不报错」的静默 bug。

---

## 题面（五问，按顺序写，不许跳）

**(1)** 给一个**严格递增**的能量网格

```
E[0] < E[1] < ... < E[n-1]
```

和一个查询能量 `e`，要求返回下标 `k`，使得

```
E[k] <= e < E[k+1]
```

**用循环写二分查找**（不许递归、不许用 `bisect`、不许用任何库）。
写在纸上，变量名自己取，但 `lo` / `hi` 的含义要在旁边用一句话写清。
for k in range(n):
    if e<=E[0]:
        lo=hi=0
    if e>=E[k]:
        if k <n-1:
            lo=k
            hi=k+1
        if k==n-1:
            lo=hi=n-1
lo=0
hi=n-1
while true:
    if (hi+1)%2==0:
        if e<E[(hi+1)/2-1]:
            if e<E[0]:
                lo=hi=0
                break
            else:
                if hi==1:
                    break
                else:
                    hi=(hi-1)/2
        if e>=E[(hi+1)/2-1]:    
            if e==E[(hi+1)/2-1]:
                    lo=(hi+1)/2-1
                    hi=(hi+1)/2
                    break
            if e>=E[n-1]:
                hi=lo=n-1
                break
            else:
                lo=(hi+1)/2-1
    if hi%2==0:
        if e<E[hi/2]:
            if e<=E[0]:
                lo=hi=0
                break
            else:
                if hi==2:
                    break
                else:
                    hi=hi/2
        if e>=E[hi/2]:   
            if e==E[hi/2]:
                lo=hi/2
                hi=hi/2+1
                break 
            if e>=E[n-1]:
                hi=lo=n-1
                break
            else:
                lo=hi/2
**(2)** 写出你的**循环不变量**：每一轮循环开始时，`lo` 和 `hi` 各保证了什么？
（提示：正确的二分之所以正确，靠的不是「感觉对」，是这句话在每一轮都成立。）
保证了e的边界

**(3)** 三个边界，你的代码各返回什么？**不许说「不会发生」**：

- `e < E[0]`（比网格最低点还低）0
- `e >= E[n-1]`（比网格最高点还高，或正好等于）n-1
- `e` **恰好等于**某个 `E[k]`k与k+1

**(4)** 复杂度：为什么是 `log2(n)`？
ENDF/B-VIII.0 的 H-1 总截面网格约 10⁴–10⁵ 个点，**最多几次比较**？
同样的查找用线性扫描要几次？**两个数都写出来。**

**(5)** GPU 视角（这题真正值钱的一问）：
一个 warp 里 32 个线程各查一个**不同**的能量。

- 这 32 个线程的二分**循环次数**一样吗？
- 它们在同一轮里走的**分支**一样吗？
- 由此说明：二分在 GPU 上比线性查找好在哪、又差在哪？
- 如果要彻底消掉这个问题，你能想到什么替代做法？

---

## 判分标准（自查，每条问自己「我写出来了吗」）

| # | 判据 | 常见失分 |
|---|---|---|
| 1 | 循环能**终止**，且返回的 k 满足 `E[k] <= e < E[k+1]` | 写成 `lo = mid` 却用下取整的 mid → **死循环**；`while lo < hi` 和 `while lo <= hi` 两套写法混着抄 |
| 2 | 说得出不变量，例如「答案永远在 `[lo, hi]` 里」 | 答「就是不断折半」——那是过程不是不变量，**证明不了正确性** |
| 3 | 三个边界**都给出明确返回值**，并说明这是「夹取」还是「报错」 | 只答「越界了就不管」；`e == E[k]` 时算左格还是右格自己也说不清 |
| 4 | 17 次 vs 10⁵ 次（10⁵ 点：`log2(1e5) ≈ 16.6`），两个数都写出来 | 只答「快很多」 |
| 5 | 指出**循环次数几乎相同（差 ≤1），但分支方向不同 → warp 内 divergence**；替代做法说得出一个 | 答「GPU 上二分很快」 |

**第 (2) 问答不上来，这道题不算过。**
你到 12 月要能当着老师的面说清「我怎么知道这段代码是对的」——
对二分查找来说，那个答案就是**循环不变量**，不是「我测了几个例子」。

---

## 三个陷阱（写完再看）

### 陷阱 1：`mid` 的取整方向决定了会不会死循环

`mid = (lo + hi) // 2` 是**下取整**，所以 `mid` 可能等于 `lo`，永远不可能等于 `hi`（当 `hi > lo` 时）。

> **推论：区间只能靠「`hi` 往下压」或「`lo = mid + 1`」缩小。
> 写出 `lo = mid` 的那一刻，你就要立刻问自己：这一步区间真的变小了吗？**

`lo=3, hi=4` 时 `mid=3`，若走 `lo = mid` → 状态原封不动 → **死循环，不报错，程序挂住**。
这和 9/2 lesson03 的 bug 7（拆层循环忘了 `k += 1`）是同一类：
**循环体没有推进状态。那次是 guard assert 救了你，这次没有 guard。**

### 陷阱 2：`(lo + hi)` 在 C++/CUDA 里会溢出

Python 的 int 无限精度，`(lo+hi)//2` 永远安全。
**但第 10–13 课你要把这段重写成 C++ 和 CUDA**，那里 `int` 是 32 位，
`lo + hi` 在大数组上会溢出成负数，`mid` 变成负下标 → 越界读 → **段错误或读到垃圾数据**。

> **现在就养成写 `mid = lo + (hi - lo) // 2` 的习惯。**
> 两种写法在 Python 里逐位相同 —— 这正好是一次「重构的正确性判据就是数字不变」的免费练习。

### 陷阱 3：错一格是**静默**的

查错一格，你插出来的截面**仍然是一个正数、量级也差不多**，不会报错、不会 NaN。

- 在光滑段（1/v 区）：错一格 → 结果差千分之几 → **你永远发现不了**
- 在共振峰上：错一格 → 结果可能差**一个数量级**

> **这就是为什么二分必须靠不变量证明对，而不是靠「跑了几个数看着对」。**
> 也是为什么第 5 课的交付判据是「**B-10 的 1/v 行为肉眼可见**」——
> 那条直线在双对数坐标上是斜率 −1/2 的直线，**查错格会让它长毛**。

---

## 闭环：你已经写过一个 O(1) 版本了

`lesson04_energy.py:99`

```python
j = int(t * M)
if j >= M: j = M-1
```

这就是同一个问题的**等距网格特例**：网格等距 ⟹ 下标可以直接算出来，不用查。

| | 等距网格 | ENDF 真实网格 |
|---|---|---|
| 定位 | `int((x-x0)/dx)`，**O(1)** | 二分，**O(log n)** |
| 为什么不能都用等距 | —— | 截面在共振区要密、在光滑区可以疏，**等距网格要么点太少不准、要么点太多爆内存** |
| 夹边界 | `if j>=M: j=M-1` | 第 (3) 问的三个边界 |

**那行 `if j >= M: j = M-1` 你 9/4 漏写了三次。**（见进度日志 9/4 bug 19）
今天这道题的第 (3) 问，问的是同一件事。

而第 (5) 问的答案，正是真实 GPU 蒙卡里 **unionized energy grid**（统一能量网格）
和**对数等距索引表**存在的理由：用一次 O(1) 的粗定位，把二分的深度从 17 层压到 2–3 层。
**这条到 12 月是可以主动讲出来的东西。**

---

## 做完记在这里

- 用时：
- 五问过了几问：
- 卡在哪一步：
- 纸上的代码敲进去跑，第一次就对了吗（三个边界各测一次）：
