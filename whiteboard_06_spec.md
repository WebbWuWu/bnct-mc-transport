# 白板题 ⑥：线性 / 对数插值

**纸笔，20 分钟，不看电脑、不看笔记。超时就停，把卡住的地方写下来。**

和 ⑤（二分查找）是一对：⑤ 告诉你 `e` 落在哪个格子，⑥ 把格子两端的值变成 `e` 处的值。
第 5 课（9/15 开工）读 ENDF/B-VIII.0 截面表时，这两个函数**是同一行代码里的两半**。

---

## 题面

给你两个等长数组：能量网格 `E[0..n-1]`（**严格递增**）和对应的截面 `S[0..n-1]`（`S[k]` 是能量 `E[k]` 处的总截面，单位 barn）。

再给你一个查询能量 `e`，且已知 `E[0] <= e <= E[n-1]`。

已经有一个函数 `find(E, e)` 返回下标 `k`，约定是 **`E[k] <= e <= E[k+1]`**（这就是 ⑤ 里 `whiteboard_05_scaffold.py` 那个约定，别改）。

---

## 五问

### (1) 线性-线性插值

写出用 `E[k]`、`E[k+1]`、`S[k]`、`S[k+1]`、`e` 表达 `S(e)` 的公式。

写完立刻做**两个端点自检**：把 `e = E[k]` 代进去应该得到 `S[k]`，`e = E[k+1]` 应该得到 `S[k+1]`。**代不出来就是写错了，当场改。**

### (2) 为什么截面表不用线性插值，要用 log-log

这一问是物理，不是数学。

想一件具体的事：**¹⁰B 的吸收截面在热能区遵循 1/v 律**，也就是 σ ∝ E^(−1/2)。

- 在**普通坐标**（σ 对 E）上，这条曲线长什么样？
- 在 **log σ 对 log E** 坐标上，它长什么样？**斜率是多少？**
- 于是「在两点之间插值」这件事，在哪个坐标系里误差小得多？为什么？

再想一步：ENDF 的能量网格从 **1e-5 eV 到 2e7 eV**，跨 **12 个数量级**。相邻两点在低能区可能只差百分之几，在高能区可能差好几倍。**这对「用直线连两点」意味着什么？**

### (3) 写出 log-log 插值公式

要求：**用 `math.log` 和 `math.exp` 写出来**，形式上和 (1) 平行（也就是「在 log 空间里做 (1)」）。

写完同样做端点自检。

**然后把它化简成幂函数形式** `S(e) = S[k] * (e/E[k])**p`，并写出 `p` 是什么。

⚠️ 化简完之后回头看 (2)：**对 1/v 律，`p` 应该等于多少？** 如果你的公式代入 1/v 的数据不能给出那个 `p`，说明推错了。**这就是这一问自带的判据。**

### (4) 三个会炸的地方

log-log 插值有三个输入会让它当场崩或者静默出错。**各写一句：什么时候发生、为什么、你打算怎么处理。**

- 提示一：`math.log` 的定义域。截面表里**真的会有 0**（某些反应道在阈能以下截面严格为 0）。
- 提示二：能量网格的第一个点。ENDF 的 `E[0]` 是 1e-5 eV，不是 0，所以这个坑在总截面表上不会踩到 —— **但你自己造测试数据时会。**
- 提示三：`E[k] == E[k+1]`。**这不是我编的**，ENDF 里**故意**放重复能量点来表示截面的**跳变**（共振的处理、阈值处）。你的公式在这里会怎么样？

### (5) 接口对齐

`find` 的约定是 `E[k] <= e <= E[k+1]`，所以合法的 `k` 范围是 `0 .. n-2`。

- 你的插值函数需不需要自己再检查一遍 `k` 的范围？**还是相信 `find`？**
- 如果检查，用 `assert` 还是 `if ... raise`？**这两者的区别是什么**（提示：`python -O` 会做什么）？

---

## 判分表

| # | 判据 | 过 |
|---|---|---|
| 1 | (1) 的公式正确，且**当场做了两个端点自检** | |
| 2 | (2) 说得出 1/v 在 log-log 上是**直线**，斜率 **−1/2** | |
| 3 | (2) 说得出跨 12 个数量级时线性插值的问题 | |
| 4 | (3) 的 log-log 公式正确 | |
| 5 | (3) 化简出幂形式，且 `p` 的表达式正确 | |
| 6 | (3) 用 1/v 反查了自己的公式 | |
| 7 | (4) 三个坑至少答对 **2 个**，且给了处理办法 | |
| 8 | (5) 说得出 assert 和 raise 的区别 | |

**6 条以上算过。** 3 条以下说明第 5 课要先补这一块再开工。

---

## ⚠️ 三个陷阱（做完再看，先看等于白做）

<details>

**陷阱 A：把 log-log 写成「对结果取 log」。**
正确的是**两个轴都取 log**：`log S` 对 `log E` 线性。只对 S 取 log（log-lin）是另一种插值律，ENDF 里也有，但那不是 1/v 用的那个。

**陷阱 B：以为 log-log 插值处处更好。**
不是。ENDF 每一段数据自带一个 **INT 标志**（1=常数, 2=lin-lin, 3=lin-log, 4=log-lin, 5=log-log），**是数据自己规定该用哪种插值**，不是你挑。第 5 课读表时要把这个标志读出来。今天不用实现全部五种，但**要知道你实现的只是其中一种**。

**陷阱 C：端点自检做成了同源检验。**
「代 `e = E[k]` 得到 `S[k]`」是有效的，因为 `S[k]` 是**独立给定的输入**，不是从公式里算出来的。但如果你的自检是「用公式算两遍看是否相等」，那就是 9/2 那条恒真 assert 的又一次。**判据的两个量必须来源独立。**

</details>
