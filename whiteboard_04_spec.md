# 白板题④ 规格书：MC 估计 π

**时间：20 分钟。纸和笔，不看电脑，不看 `whiteboard_04_background.md`。**
**满分 8 分。**

---

## 题面

用蒙特卡洛方法估计 π，并给出误差棒。

**约定死的设定**（不要换，换了后面几问对不上）：

```
框     ： 单位正方形 [0,1] × [0,1]
目标区 ： 四分之一圆盘  x² + y² < 1
撒点   ： 两个独立的 U[0,1)
```

---

### (1) 证明 —— 1.0 分（2 分钟）

写出估计量 `pi_hat` 的表达式，并证明 `E[pi_hat] = π`。

要求：

- **明确写出你用到的那条关于均匀分布的基本性质**（一句话）；
- **指出它在你的推导的哪一步被用到**。

⚠️ 论据里不许出现结论。（9/16 白板题⑦(1) 踩过：用「均匀」证「均匀」。）

---

### (2) 误差棒 —— 1.5 分（4 分钟）

- **(2a)** 求 `Var(pi_hat)` 和 `SE(pi_hat)` 的闭式。写成只含 `N` 和 π 的形式，
  并算出 `N = 10⁶` 时的数值（两位有效数字即可，π ≈ 3.1416）。
- **(2b)** 要让 `pi_hat` 的 1σ 误差小于 `5e-4`（即「小数点后三位可信」），`N` 要多大？
- **(2c)** 再多要一位小数，`N` 要乘以几？这个倍数和第 4 课自测题 1 的结论是不是同一件事？
  **用一句话说出它们为什么是同一件事。**

---

### (3) 手写函数 —— 1.5 分（4 分钟）

在纸上写出 `estimate_pi(rng, N)`，返回 `(pi_hat, se)`。

硬约束（违反一条扣 0.3）：

- **随机性从哪来**：函数里不许出现 seed，不许自己造 rng。
- **不许出现任何可以从参数算出来的硬编码数字。**
- 至少一条 assert，**条件和消息必须说同一件事**。
- 返回的 `se` 必须是**从样本算出来的**，不许直接套 (2a) 的闭式。
  （理由你自己想，(4) 会用到。）

---

### (4) 判据 —— 1.5 分（4 分钟）

为 `estimate_pi` 设计判据。要求：

- **至少两条，不超过 5 条**；
- **每条旁边写一行「这条抓什么」**；
- **至少一条是确定性判据**（给定输入必然给定输出，不涉及统计），
  并说明为什么光有统计判据不够；
- 对每一条回答：**它的两边是不是来自两条独立的路？** 如果不是，改到是为止。

⚠️ 「运行时防护」不算判据（9/17 白板题⑧(4) 踩过：交的三条 assert 在一个完全写错的函数里也全过）。

---

### (5) 换个框 —— 0.5 分（2 分钟）

把框换成 `[-1,1] × [-1,1]`、目标区换成整个单位圆 `x² + y² < 1`。

- **(5a)** `pi_hat` 的表达式要怎么改？
- **(5b)** 如果只改了撒点范围、**忘了改表达式**，会怎样？**程序报不报错？**
- **(5c)** 两种框，哪个的方差小？（只要结论 + 一句理由）

---

### (6) GPU —— 1.0 分（3 分钟）

- **(6a)** 这个 kernel 有没有 warp divergence？如果有，**指出是哪一行**。
- **(6b)** 和白板题⑧ 那个「p = 0.785 的舍选，32 线程 warp 要 3.13 轮而单线程 1.27 轮」相比，
  **这里的代价是不是同一笔账？** 回答「是」或「不是」，并说明**判别依据是什么**。
  ⚠️ 不许直接搬 9/17 / 9/18 的倍数。
- **(6c)** 累加那一步（把 0/1 加进一个总数）在 GPU 上会遇到什么问题？只要名字 + 一句话。

---

### (7) 另一种估计量 —— 0.5 分（2 分钟）

同一个 π，还可以这样估：

```
pi_hat_2 = (4/N) · Σ sqrt(1 - x_i²)      x_i ~ U[0,1)
```

- **(7a)** 这个估计量无偏吗？一句话理由。
- **(7b)** 它和命中法哪个方差小？**给理由，不要求算到具体数。**

---

### (8) 自审 —— 0.5 分（2 分钟）

**交卷前的最后一步，必须做：**

把 (1)–(7) 的**结论句**单独抄成一列（五到八行），
**只读这一列，不看推导**，找互相矛盾的。

写下：

- 你抄出来的那一列；
- 你找到的矛盾（**如果一处都没找到，也写「0 处」**）。

> 记录：⑦ 的 (8) 是 0.5 / 0.5（两处全中），⑧ 是 0 / 0.5，③ 是 0 / 0.5（而纸上有五处）。

---

## 交卷后

拍照或抄进对话框，我判分。**判完再动电脑。**
