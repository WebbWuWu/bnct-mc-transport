# 白板题③ 各向同性方向抽样

**规则**：纸上手写，**不看电脑、不看 `lesson02_direction.py`、不看笔记**，20 分钟。
**开考前先读 `whiteboard_03_background.md`。**
**这份规格书保持干净，不要在上面写答案**（9/9 教训：`whiteboard_06_spec.md` 被写成了答题纸，复训时没法再用）。

**满分 8 分。**

---

## (1) 证明　【2.0 分】

证明：`μ` 均匀抽在 `[−1, 1]`、`φ` 均匀抽在 `[0, 2π)`，按背景 §3 组装出来的 `Ω`
**确实**均匀分布在单位球面上。

要求：

- 明确写出你用到的那条**关于均匀分布的基本性质**（9/16 那条地基）
- ⚠️ **论据里不许出现结论**（9/16 白板题⑦(1) 的教训）。写完自问：**我这一步用到的东西，是不是还没被证明过？**

---

## (2) 错法错在哪　【1 分】

(2a) 如果按「θ 均匀抽 `[0, π]`」去做，**`μ = cos θ` 的概率密度 `p(μ)` 是什么？** 写出表达式。

(2b) 用 (2a) 说明粒子堆在哪里、堆得有多厉害。

(2c) 只看 `<w>` 和 `<w²>` 这两个数，**能不能发现这个错？** 为什么？

---

## (3) 写函数　【1.5 分】

在纸上写出

```
isotropic_direction(rng) -> (u, v, w)
```

要求：

- **9/16 那条纪律**：随机性从哪来，是调用方的事还是函数的事？想清楚再动笔
- 开平方那一步**为什么取正根**，旁边写一行注释说清楚
- 写完，回头数一遍你用的每个变量在后面被用到了几次

---

## (4) 判据 —— 只答一问　【0.5 分】

> 🔴 **2026-09-22 改版。** 原 (4) 要求你自己设计三条判据（1.5 分）。按现行分工
> **判据和 assert 由 Claude 给能直接粘贴的成品**，所以这一问降为 **0.5 分**，
> 只保留其中真正属于你的那一半：**物理上哪个量才有分辨率**。
> 三条判据成品在判分之后由 Claude 给出。
> 让出的 1.0 分：(1) 1.5 → **2.0**，(6) 1.0 → **1.5**。

**8/31 那天，唯一暴露出「θ 均匀抽」这个错的是哪一个量？**

写出这个量本身，并回答两问：

- **(4a)** 为什么是它？`<w>` 为什么抓不住？（这一条要和你 (2c) 的答案对得上）
- **(4b)** 要判出这个错，大致需要多少条历史？给一个**数量级**，并写出你是怎么估的。
　　 ⚠️ 提醒：**标准差 s 是单条历史的散布，标准误 SE = s/√N 才是均值的误差。** 别混。

---

## (5) 浮点边界　【0.5 分】

开平方的参数 `1 − μ²` 有没有可能变成负数？

- 若不可能，**依据是什么**？（提示：9/6 核对过的那个区间）
- 若可能，什么输入会触发？

---

## (6) GPU 代价　【1.5 分】

🔴 **9/16 纪律：「会不会发散」和「代价有多大」是两个问题。只答第一个不得分。**

(6a) 这段抽样在一个 32 线程的 warp 里**会不会 divergence**？为什么？
　　 **和昨天的舍选法对比着答** —— 两者在这一点上是相反的，说清楚为什么相反。

(6b) 既然如此，它在 GPU 上的代价主要花在哪？**数一数**：一次调用里
　　 平方根、余弦、正弦、除法各几次，随机数几个。

(6c) 背景 §7 给了一条**用舍选法去掉三角函数**的路。把它补完：
　　 - 写出抽样步骤
　　 - **算出接受率**（方法和昨天 (2a) 一样，是一个面积比）
　　 - 用 (6a) 的结论判断：这条路在 warp 里**是不是又把发散引回来了**？划不划算？

---

## (7) 一维平板里 φ 抽不抽　【0.5 分】

`lesson03_tally.py` 里粒子只沿 z 走（`z += s*w`），`u` 和 `v` **一次都没被用到**。

(7a) **物理上**：不抽 `φ`，最终的通量结果会不会变？为什么？

(7b) **判据上**：今天步骤 4 的交付判据是「单能极限下与 `lesson03_tally.py` **逐位相同**」。
　　 不抽 `φ` 这个改动，会不会影响这条判据？为什么？

(7c) (7a) 和 (7b) 的答案**是不是一致的**？如果不一致，**这说明了什么**？

---

## (8) 自审 —— 一致性　【0.5 分】

🔴 **这一问考的是 9/17 立的那条新纪律：自审的第一刀不是「我答得对不对」，是「我这几个答案彼此对得上吗」。**

**不看上面任何提示**，把你 (1)(2)(4)(6)(7) 的答案摆在一起逐对核对：

- 找出**至少一处**互相对不上的地方，写清楚是哪两句、为什么不可能同时对
- **如果确实找不到**，就列出**至少三对**你核对过的、彼此一致的答案，并写明「它们一致体现在哪个共同的量上」

⚠️ 「我觉得我是对的」不得分（9/17 (8) 就是这么丢的 0.5）。

---

## 判分后要接的事

答完拍照或贴出来给 Claude 判分。
**判分后 Claude 给 (4) 的三条判据成品**（可直接粘贴，含每条「这条抓什么」和适用条件）。
然后进今天第二段：**默写演练 90 分钟**（`lesson03_tally.py`，不看代码）。
(7) 的答案**是第 6 课步骤 1 的一半** —— 别扔。
