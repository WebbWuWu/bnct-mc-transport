# 白板题② —— 自由飞行距离的逆变换抽样

**出题日**：2026-09-06（8/31 起挂账，今天补）
**形式**：白纸、黑笔、**不看任何东西**，限时 10 分钟。写完再对判分标准。
**为什么是这题**：12 月冬令营老师必问的五条里的第一条 ——「为什么是 `-ln(1-ξ)/Σt`」。

---

## 题面（五问，按顺序写，不许跳）

**(1)** 中子在均匀介质中从当前位置出发，写出自由飞行距离 `s` 的**概率密度** `f(s)`。
**不是背公式** —— 要把它拆成两个因子，并各用一句话说清它是什么：
一个是「活到 s 还没作用」的概率，一个是「恰好在 `[s, s+ds]` 里作用」的概率。

**(2)** 由 `f(s)` 求**累积分布函数** `F(s)`，写出积分过程。

**(3)** 用**逆变换法**从 `F(s)` 推出抽样公式。写到

```
s = -ln(1 - ξ) / Σt ,     ξ ~ U(0,1)
```

**要写推导，不许直接写结论。**

**(4)** 说明为什么实际代码里常写成 `s = -ln(ξ) / Σt`。
这个替换**在什么条件下合法**？**在什么情况下会炸**？
（提示：想想你自己写的 LCG 生成的区间到底是 `[0,1)` 还是 `(0,1]`。这不是理论问题，是你代码里的问题。）

**(5)** 逆变换法**成立的条件**是什么？它失效的时候用什么代替？
举一个你在这个项目里迟早会遇到的、**不能用逆变换法**的分布。

---

## 判分标准（自查，每条问自己「我写出来了吗」）

| # | 判据 | 常见失分 |
|---|---|---|
| 1 | `f(s) = Σt · exp(-Σt·s)`，且**两个因子分别说清了** | 只写结果；把 `Σt` 说成「概率」 |
| 2 | `F(s) = 1 - exp(-Σt·s)`，有积分过程 | 直接抄记忆 |
| 3 | 令 `F(s) = ξ` **反解**，过程完整 | 跳步；把 `ξ` 和 `1-ξ` 弄反却不自知 |
| 4 | 说清 `ξ` 与 `1-ξ` **同分布**所以可换，**且**指出端点风险 | 只说「因为对称」，说不出哪个端点会炸 |
| 5 | 说出 F **必须能解析求逆**；替代是**舍选法**；举出一个具体例子 | 答不上例子 |

**第 (1) 问里「Σt 不是概率、是每单位长度的作用概率（cm⁻¹）」这句话如果没写出来，这道题就算没过。**
这是 9/2 立的教学点，也是面试第二问的原题。

---

## 端点陷阱（第 4 问的核心，写完再看）

`ln` 在 0 处是 `-inf`。所以：

- 写 `-ln(ξ)`：`ξ = 0` 会炸 → 要求 RNG **取不到 0**
- 写 `-ln(1-ξ)`：`ξ = 1` 会炸 → 要求 RNG **取不到 1**

**哪一种写法是安全的，完全取决于你的 `lcg` 生成的是哪个半开区间。**
这不是数学问题，是**你自己那个 `next_double()` 的实现细节**。
→ **收工前回去看一眼 `lesson01_lcg.py`，确认你现在用的写法和你的区间是配套的。**

（这是「静默失效」的又一个标准形态：`ξ` 恰好取到端点的概率约 `2⁻³²`，
跑一亿条历史大概撞一次。**它不会在你调试时出现，只会在你演示时出现。**）

---

## 闭环：这题你其实已经算过一遍了

第 4 课 A=1（氢）的 4a 里，能量比 `E'/E ~ U(0,1)`。于是

```
x = -ln(E'/E)  ~  Exp(1)
```

**这就是逆变换法本身，只是你当时是从物理那头走过来的。**
50 次散射的 lethargy 之和 `Σx ~ Gamma(50, 1)`，均值 50、方差 50 ——
9/5 那张表里 `4a Wald = 49.99 (SE 3.15e-2)`、`4a' var = 0.991` 就是这两个数。

**同一个数学对象，你从两个方向各碰到了一次。白板上要能主动把这两头连起来讲。**

---

## 做完记在这里

- 用时：
- 五问过了几问：
- 卡在哪一步：
