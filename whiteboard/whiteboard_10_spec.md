# 白板题⑩ 规格书：隐式俘获与权重

**时间：25 分钟。纸和笔，不看电脑，不看 `whiteboard_10_background.md`，不看代码。**
**开考前先读 `whiteboard_10_background.md`。**
**这份规格书保持干净，不要在上面写答案**（9/9 教训：⑥ 被写成了答题纸，复训时废了一张题）。

**满分 8 分。**

**记号约定**（背景 §1）：`wt` = 权重，`w` = 方向余弦。**两个别混。**

---

## (1) 无偏性证明　【2.0 分】（6 分钟）

设某次碰撞之后，粒子在余下的一生里还会得到的总分是随机变量 `S`，`p = Σs/Σt`。

- **(1a)** 写出**类比**做法下，这次碰撞对「后续得分」的期望 `E[类比]`，用 `p` 和 `E[S]` 表示。
  **把你用到的那条关于期望的性质写出来**（就是「以概率 a 拿到 X、以概率 1−a 拿到 Y」那条）。
- **(1b)** 写出**隐式俘获**做法下的期望 `E[隐式]`。注意权重是乘在**得分**上的。
- **(1c)** 比较两者，下结论。

⚠️ **论据里不许出现结论。** 写完自问：**我这一步用到的东西，前面证过了吗？**
⚠️ **(1a) 里那个「以概率 1−p 得 0」的 0，是从哪来的？** 说清楚它为什么是 0 而不是别的。

---

## (2) 方差与一个算例　【1.5 分】（5 分钟）

用背景 §4 那个玩具模型：**`p = 0.8`，问「连续 `n = 10` 次碰撞后还剩多少」**。

- **(2a)** 类比做法下，得分 `X` 的**期望**和**方差**各是多少？给三位有效数字。
  （提示：`X` 是指示变量，白板题④ 那条 `Var = P(1−P)`。`0.8¹⁰ ≈ 0.1074`。）
- **(2b)** 隐式做法下，得分 `X` 的期望和方差各是多少？
- **(2c)** 类比要跑多少条历史，才能把相对误差 `R` 压到 **5%**？给数字，写出你用的式子。
- **(2d)** 🔴 **这个玩具模型省略了什么，使得 (2b) 的答案在真实程序里不成立？**
  说出**至少一样**真实程序里仍然随机、而模型里被拿掉了的东西。

---

## (3) 写函数　【1.5 分】（5 分钟）

在纸上写出：

```
collide_implicit(wt, sigma_s, sigma_t)  ->  wt_new
```

以及**改动后的打分那一行**（原来是 `track_h[k] += dz_step/abs(w)`）。

硬约束（违反一条扣 0.3）：

- **随机性从哪来？** 这个函数里要不要有 `rng`？**想清楚再动笔**（纪律 18，白板题③ (3) 刚考过）。
- **不许出现任何可以从参数算出来的硬编码数字。**
- **不写 assert**（assert 由 Claude 给成品）。
  但用一句话写出：**什么样的输入会让这个函数出事？**
- 写完，**回头数一遍你用的每个变量在后面被用到了几次。**

---

## (4) 判据 —— 只答一问　【0.5 分】（2 分钟）

> 按现行分工，**判据和 assert 由 Claude 给成品**，所以这一问只保留属于你的那一半：
> **哪个量有分辨率。** 三条判据成品在判分之后给你。

**假设你把 (3) 写成了 `wt * (sigma_a/sigma_t)`（用了吸收截面，符号搞反）。**

- **(4a)** 跑一次纯吸收算例（`Σs = 0`），这个错**会不会**当场露出来？为什么？
- **(4b)** 换成 `Σs/Σt = 0.8` 的散射算例，**哪一个量**最先暴露它？为什么是它？

---

## (5) 它会打掉哪些判据　【1.5 分】（5 分钟）

🔴 **这一问分值最集中的地方不在"会不会失效"，在"为什么"。**

对下面四条，各答「仍然成立 / 失效 / 需要改写」，**并各写一行理由**：

- **(5a)** `T + R + A = 1`
- **(5b)** 判据 5：「散射消耗 4 个随机数 / 吸收 2 个 / 飞出 1 个」
- **(5c)** 验证 B：`Σt · Σ_k track_sum[k] − n_碰撞 == 0`
- **(5d)** `n_stuck`（撞满 `Max = 10000` 次判为异常）

**(5e)** 这四条里，**哪一条失效了却不会报错、也不会让结果变得离谱**？
　　 —— 也就是说，**哪一条是最危险的？** 一句话说为什么。

---

## (6) GPU 代价　【0.5 分】（2 分钟）

🔴 **9/16 纪律：「会不会发散」和「代价有多大」是两个问题。只答第一个不得分。**

类比里每条历史何时结束是随机的；隐式里每条历史都活到你主动截断为止。

- **(6a)** 一个 32 线程的 warp 里，**哪一种的发散更严重**？为什么？
- **(6b)** 用今天 (6c) 学到的算法：判断发散代价，要拿**平均值**还是**32 个里最慢的那个**？一句话。

---

## (7) 自审 —— 一致性　【0.5 分】（2 分钟）

**不看上面任何提示**，把 (1)(2)(4)(5)(6) 的结论句单独抄成一列，**只读这一列**，找互相矛盾的。

- 找出**至少一处**对不上的地方，写清楚是哪两句、为什么不可能同时对；
- **如果确实找不到**，列出**至少三对**你核对过的、彼此一致的答案，**并写明它们一致体现在哪个共同的量上**。

⚠️ 「我觉得我是对的」「均一致」不得分。
🔴 **方法（9/21 定）：先挑出写了定义的行，拿它逐条去对别的行。**
　 记录：⑧ ③ ④ ⑨ 四次报 0 处，今天 ③ 复训第五次。

---

## 判分后要接的事

答完拍照或贴出来给 Claude 判分。
**判分后 Claude 给 (4) 的三条判据成品**（可直接粘贴，含每条「这条抓什么」和适用条件）。
**(5) 的答案直接是第 8 课开工当天要用的清单 —— 别扔。**
下一题是**⑪ 证明轮盘赌无偏**（9/24），它接着这一题的 §5 代价①往下走。
