# 白板题⑨ 规格书：样本方差与相对误差

**时间：25 分钟。纸和笔，不看电脑，不看 `whiteboard_09_background.md`。**
**满分 8 分。**

下面几问要用到你自己的 `mcstat.py`，原样抄在这里（行号是文件里的真实行号）：

```python
 3  def mean_se(s,sq,n):
 4      """由 Σx、Σx²、n 算样本均值、贝塞尔方差、均值标准误。"""
 5      assert n>1,\
 6          "n取值不对，n=%d"%n
 7      mean=s/n
 8      var=n/(n-1)*(sq/n-mean**2)
 9      assert var>=0,\
10          "var范围出错，var=%.6f"%var
11      se=math.sqrt(var/n)
12      return mean,var,se
```

---

### (1) 手算 —— 1.0 分（4 分钟）

五条历史的得分：`x = [0, 0, 3, 1, 1]`。

- **(1a)** 算 `x̄`、`s²`（带贝塞尔）、`SE`、`R`。写成分数或三位有效数字。
- **(1b)** 再算**不带贝塞尔**的方差。它和 `s²` 的比值是多少？**一句话说这个比值从哪来。**
- **(1c)** 能不能拿讲义 §4 那条 `1/√(2(n−1))` 估一下 (1a) 这个 SE **自己**差多少？
  回答「能」或「不能」，**并说出你判断的依据**。

---

### (2) 读表找错 —— 1.5 分（3 分钟）

某人用 `Σt = 0.5` 估平均飞行距离（真值 2），交上来这张表：

```
N       x̄        SE
10⁴    2.0031   2.00e-4
10⁵    1.9994   2.00e-5
10⁶    2.0002   2.00e-6
```

- **(2a)** 这张表哪里不对？**只看这三行就能下结论，说出你看的是哪个量、它应该怎样、实际怎样。**
- **(2b)** 如果错在 `mean_se` 里，**最可能是哪一行、被写成了什么样？**把那一行写出来。
- **(2c)** `mcstat.py` 自带的自测是这样的：

  ```python
  45      mean, var, se = mean_se(s, sq, 4)
  46      assert abs(var - 5/3) < 1e-12, "自测1方差不合格，var=%.6f"%var
  ```

  **它能不能抓住 (2b) 那个错？**为什么？（只要回答 + 一句理由。补的那条 assert 由 Claude 给成品，你不用写。）

---

### (3) 手写函数 —— 1.0 分（4 分钟）

在纸上写出 `rel_fom(s, sq, n, t)`，返回 `(mean, se, R, fom)`。`t` 是这次运行用的秒数。

硬约束（违反一条扣 0.3）：

- **必须调用 `mean_se`**，不许自己再算一遍均值和 SE。
- **不许出现任何可以从参数算出来的硬编码数字。**
- **不写 assert**（assert 由 Claude 给成品）。但在旁边用一句话写出：
  **什么样的输入会让算 `R` 出事，什么样的输入会让算 `fom` 出事？**

---

### (4) 分批 —— 1.5 分（5 分钟）

同一次运行，`N = 10⁶`。两种算法给出：

```
按历史算（10⁶ 个样本喂 mean_se）         SE_h = 1.00e-3
分 10 批、每批 10⁵（10 个批均值喂 mean_se）  SE_b = 1.19e-3
```

- **(4a)** `SE_h` 和 `SE_b` 在估**同一个量**吗？是的话，**说出是哪个量**（一句话）。
  （纪律 2：先确认在比同一个量。）
- **(4b)** 两者差 19%。**这是 bug 吗？** 写出你的判断依据：用一个公式算出「差多少以内算正常」，
  并**写出这个公式的适用条件**，说明这里满足不满足。
- **(4c)** 想让 `SE_b` **自己**的相对误差小于 5%，至少要分几批？每批还剩多少条历史？
- **(4d)** 从 10 批改成 (4c) 的批数，`x̄` 的 SE 会变小吗？一句话。

---

### (5) 浮点 —— 1.5 分（4 分钟）

某个量的单位是 eV，三条历史的得分是：

```
x = [1e8, 1e8 + 1, 1e8 + 2]
```

- **(5a)** 手算真正的 `s²` 和 `SE`。
- **(5b)** `mean_se` 的**哪一行**在这组输入上有危险？**用数量级说明为什么**
  （提示：`sq` 大约多大？那附近相邻两个 double 差多少？）
- **(5c)** 程序实际会怎样？三选一，**给理由**：
  A. 第 9 行 assert 响　B. 不报错，但给出一个错的 `s²`　C. 给出对的 `s²`
- **(5d)** 第 9 行 `assert var>=0` 能不能保证 `var` 是对的？**它挡住的是哪一类错、放过的是哪一类？**
- **(5e)** 如果第 12–14 课改用 `float32`，得分在**什么数量级**就开始危险？给数量级 + 依据。
  （只要思路，**不写代码**；怎么改 `mean_se` 也只要一句思路。）

---

### (6) FOM —— 1.0 分（3 分钟）

同一个问题，两个程序：

```
A (CPU)   N = 10⁷    R = 1.0e-2    T = 100 s
B (GPU)   N = 10⁷    R = 2.0e-2    T =  10 s
```

- **(6a)** 按「时间」B 比 A 快几倍？按 FOM 呢？**两个数都写出来。**
- **(6b)** 同样 `N`，`R` 却差一倍。**这说明什么？** 说出一个你要先去排查的原因。
  （想想 Neudep 表 2 那个 64 倍。）
- **(6c)** 如果 A 每次运行开头要先花 60 s 读截面库，用 `N = 10⁶` 跑出来的 FOM 会比 `N = 10⁷` 的**大还是小**？
  这违反了讲义 §3 的哪一条适用条件？

---

### (7) 自审 —— 0.5 分（2 分钟）

**交卷前的最后一步，必须做：**

把 (1)–(6) 的**结论句**单独抄成一列（六到十行），
**只读这一列，不看推导**，找互相矛盾的。

写下：

- 你抄出来的那一列；
- 你找到的矛盾（**如果一处都没找到，也写「0 处」**）。

> 记录：⑦ 的这一问是 0.5 / 0.5（两处全中），⑧ ③ 是 0，④ 是 0.1（报 0 处，实际 3 处）。

---

## 交卷后

拍照发过来，我判分。**判完再动电脑。**
